// singleton 
// constructor method 
Object.create

// object literals


const jsUser = {}  // bn gya object , yh ak object hai   

// objects are based on key value 

const user = {
    name : "CP",
    age : 22,
    location: "Delhi",
    //  isme hm arrays ko bhi declare kr skte hai|
    lastLoggedIn:["mon" , "tue"],
}

// accessing of object 
console.log(user.name);
console.log(user["name"]);  // anotyher way to acces the elements
